/**
 * Returns the current date
 * @returns {Date} - Returns the current date
 * 
 * @example
 * ```typescript
 * console.log(getCurrentDate()); // If today is 2022-01-01, this will output a Date object representing 2022-01-01
 * ```
 */
export default function getCurrentDate(): Date {
  return new Date();
}
